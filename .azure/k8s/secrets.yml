# IMPORTANT: This is a template. Do NOT commit actual secrets!
# Use Azure Key Vault integration or kubectl create secret command
apiVersion: v1
kind: Secret
metadata:
  name: oms-secrets
  namespace: oms-dev
type: Opaque
stringData:
  # Replace with actual values using Azure Key Vault or sealed secrets
  JWT_SECRET: "REPLACE_WITH_ACTUAL_SECRET_FROM_KEYVAULT"
  DB_USERNAME: "REPLACE_FROM_KEYVAULT"
  DB_PASSWORD: "REPLACE_FROM_KEYVAULT"
---
# Azure Key Vault integration (recommended)
# apiVersion: secrets-store.csi.x-k8s.io/v1
# kind: SecretProviderClass
# metadata:
#   name: oms-keyvault-secrets
#   namespace: oms-dev
# spec:
#   provider: azure
#   parameters:
#     keyvaultName: "oms-keyvault-dev"
#     objects: |
#       array:
#         - |
#           objectName: jwt-secret
#           objectType: secret
#         - |
#           objectName: db-password
#           objectType: secret
#     tenantId: "YOUR_TENANT_ID"
