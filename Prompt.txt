
1. You are my end-to-end SDLC automation agent. Build a complete Order Management system using:

Backend:
- Java + Spring Boot microservices
- Runtime: Tomcat (explicitly)
- API Gateway: Spring Cloud Gateway (preferred) or a simple edge service

Frontend:
- React Native Web app (React Native components rendered on Web using react-native-web)
- Use Expo for Web so the app can run in a browser and be exported as a web build

Docs & Diagram:
- Architecture diagram must be created in draw.io format (valid draw.io XML) and saved as .drawio

Goal:
Demonstrate SDLC from requirements → planning → architecture → implementation → tests → documentation → demo script.

Architecture requirements (microservices):
Create at least these services (each as a separate Spring Boot module/project):
1) api-gateway
2) auth-service (JWT auth + roles)
3) order-service (order lifecycle)
4) catalog-service (products)
5) inventory-service (reserve/release stock)
6) payment-service (mock provider)
7) shipping-service (mock provider)
8) notification-service (optional mock)

Communication:
- REST synchronous calls for simplicity (optional: event-driven enhancement)
- Protect service-to-service calls (JWT or internal token)

Persistence:
- Each service owns its own data/schema (Postgres recommended; allow H2 for local demo)
- Provide docker-compose for local run (services + dbs as needed)

Tomcat requirement:
- Provide BOTH modes and document them:
  A) Local dev: run as executable (embedded) for quick demo
  B) External Tomcat: produce deployable WAR artifacts and document deployment steps

Frontend (React Native Web with Expo):
- Minimal but usable web screens:
  - Login
  - Product list
  - Create Order (select product + qty)
  - Order list + status tracking
- Use a configurable API base URL (env/config file)
- Must run in browser using Expo web workflow and support a production web export

SDLC Deliverables: create these files under /docs
A) /docs/01-requirements.md  (functional + non-functional requirements)
B) /docs/02-backlog.md       (epics + user stories + acceptance criteria + definition of done)
C) /docs/03-architecture.md  (microservices view + data flow + deployment view + security)
D) /docs/04-api-spec.md      (API contracts per service)
E) /docs/05-test-strategy.md (unit/integration/contract tests + coverage goals)
F) /docs/06-runbook.md       (local setup + docker-compose + env vars + how to run)
G) /docs/07-demo-script.md   (7-minute spoken demo steps)
H) /docs/order-management-architecture.drawio (VALID draw.io XML importable diagram)

Execution:
1) First generate requirements + backlog (A & B)
2) Then generate architecture + API spec (C & D)
3) Pause and summarize the planned system (services, responsibilities, DBs, main endpoints, web UI pages)
4) Then scaffold repos/modules and implement the services
5) Implement the React Native Web frontend using Expo web
6) Add tests and make them pass
7) Finalize docs and demo script

Constraints:
- Enterprise-ready: validation, error handling, structured logs, health endpoints
- Demo-friendly: simple flows, minimal dependencies, clear run commands
- Save everything in repo; do not rely on external paid services

1.1. 
List each microservice and summarize:
- responsibilities
- owned data (tables/schemas)
- exposed APIs
- dependencies on other services
Cite file names/modules for your conclusions.

1.2

Explain the end-to-end request flow for "Create Order":
Web UI → Gateway → Auth → Order → Inventory → Payment → Shipping → status update.
Include how JWT is validated and how service-to-service calls are protected.
Cite files where configuration exists.

1.3

Summarize the React Native Web frontend:
- main pages/screens and navigation
- API client structure and base URL config
- auth token storage approach
Cite key files and folders.

1.4

Describe the deployment topology including Tomcat:
- which services are packaged as WAR vs runnable JAR for local dev
- how WARs deploy to external Tomcat
- ports, env vars, docker-compose mapping
Cite file paths.

1.5

Describe the deployment topology including Tomcat:
- which services are packaged as WAR vs runnable JAR for local dev
- how WARs deploy to external Tomcat
- ports, env vars, docker-compose mapping
Cite file paths.

2. Create a high-quality architecture diagram for this Order Management solution in draw.io format.

Output requirements:
1) Create file: /docs/order-management-architecture.drawio
2) The file must be valid draw.io XML (import directly into draw.io).

Diagram must include swimlanes/groups:
- Web Client (React Native Web / Expo)
- Edge / Gateway
- Microservices (auth, order, catalog, inventory, payment, shipping, notification optional)
- Data Stores (per service DB/schema)
- External Integrations (mock payment gateway, mock shipping provider)
- Observability (logs/metrics placeholder)

Must show Tomcat deployment:
- depict services deployed on Tomcat (external) AND note local dev mode (embedded) in the legend.

Key flows to visualize (number the arrows):
Flow 1: Login → JWT
Flow 2: Browse Products
Flow 3: Create Order → Reserve Inventory → Capture Payment → Create Shipment → Update Status

After creating the file:
- Provide a legend
- Provide 8–10 bullets describing what the diagram communicates.

3.
Update /docs/06-runbook.md with a clear "Frontend (React Native Web)" section that includes:
- how to install web dependencies for Expo web (react-dom, react-native-web, @expo/metro-runtime)
- how to run the web app in browser (npx expo start --web)
- how to export a production web build (npx expo export --platform web)
Also include backend and Tomcat deployment sections as already planned.



